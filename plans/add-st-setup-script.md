# Add st Setup Script

- [x] Add regression coverage for the scripted `st` setup/install workflow.
- [x] Create a repo-owned script that renders the tracked `st` template into `~/.config/st/config.def.h`, syncs it into a source checkout, and installs it.
- [x] Document the script in the README command reference and add a manual smoke test for the build/install path.
- [x] Run the targeted test and review the focused diff.
