interface:
  display_name: "README Recruiter Sync"
  short_description: "Keep the root README recruiter-first, commit-ready, and repo-aligned."
  default_prompt: "Use $readme-recruiter-sync to enforce a recruiter-first gate: require a substantial opening hook + impact, a repo-based Tech Stack And Why Chosen before setup, and clear installer/day-to-day/CLI docs before passing."

policy:
  allow_implicit_invocation: true
