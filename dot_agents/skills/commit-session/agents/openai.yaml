interface:
  display_name: "Commit Session"
  short_description: "Commit and push only current-session git work"
  default_prompt: "Use $commit-session to commit and push only the git changes dirtied during the current Codex session, while leaving unrelated baseline-dirty files untouched."

policy:
  allow_implicit_invocation: false
