interface:
  display_name: "Commit Dirty"
  short_description: "Verify, commit, and push the intended dirty git worktree"
  default_prompt: "Use $commit-dirty to verify the current repo changes, create a clean commit, and push the active branch immediately after the successful commit unless the user explicitly says not to."

policy:
  allow_implicit_invocation: false
